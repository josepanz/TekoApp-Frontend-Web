apiVersion: v1
kind: Service
metadata:
  name: service-tekoapp-frontend-web
  namespace: default
spec:
  type: ClusterIP
  ports:
    - port: 10006
      targetPort: 3001
      name: service-tekoapp-frontend-web
  selector:
    app: deploy-tekoapp-frontend-web
